numpy
opencv-contrib-python
pillow
pyzbar
zxing-cpp
rembg
onnxruntime
rawpy
pywebview
requests
pytest
# optional, only if the SAM cutout engine is used:
# mobile-sam
# optional, only for the OCR upright fallback:
# pytesseract
# test-only (generates barcodes for the decoder tests):
python-barcode
# build-only (desktop exe packaging):
pyinstaller
